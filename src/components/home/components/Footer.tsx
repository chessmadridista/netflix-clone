import { Grid } from "@mui/material";

function Footer(): JSX.Element {
    return (
        <Grid container sx={{color: '#fff'}}>
            <Grid item sx={{background: 'blue'}} xs={12}>
                asd
            </Grid>
        </Grid>
    );
}

export default Footer;